trigger ContactTrigger on Contact (after insert) {
    /* switch on Trigger.operationType {

         when AFTER_INSERT {
             if(PAD.canTrigger('ZuoraSubscriptionActions')) {
                 // ZuoraSubscriptionActions.createDistributorAccount(trigger.new);
             }
         }

       }*/
}